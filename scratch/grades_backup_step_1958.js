"import { calculateMatchPercentage } from './cosineSimilarity';\n\n/**\n * Calculates the comprehensive readiness percentage for Target Lock mode.\n * Combines 3 factors:\n *   - Skill Match (50%): cosine similarity between user's skill vector and target benchmark\n *   - Portfolio Score (25%): based on number of activities checked (max 10 = 100%)\n *   - Self-Assessment Score (25%): average of self-assessment ratings (1-5 scale)\n * \n * This ensures that portfolio and self-assessment actually affect the readiness gauge,\n * unlike the raw calculateMatchPercentage which only uses skillVector.\n *\n * @param {number[]} skillVector - User's 5-dimension skill vector [logic, science, language, art, management]\n * @param {number[]} benchmark - Target path's benchmark vector\n * @param {string[]} portfolio - Array of portfolio activity strings the user checked\n * @param {Object} selfAssessment - Object mapping subject IDs to ratings (1-5)\n * @returns {number} - Readiness percentage (0-100)\n */\nexport function calculateReadiness(skillVector, benchmark, portfolio, selfAssessment) {\n  // Factor 1: Skill Match (50% weight) — from grades + assessment responses\n  const skillMatch = calculateMatchPercentage(skillVector, benchmark);\n\n  // Factor 2: Portfolio Score (25% weight)\n  // More activities = better preparation. Cap at 10 items for 100%.\n  const portfolioCount = (portfolio || []).length;\n  const portfolioScore = Math.min(100, Math.round((portfolioCount / 10) * 100));\n\n  // Factor 3: Self-Assessment Score (25% weight)\n  // Average of self-assessed confidence ratings (1-5), converted to 0-100%.\n  const saValues = Object.values(selfAssessment || {});\n  const saAvg = saValues.length > 0\n    ? saValues.reduce((sum, val) => sum + val, 0) / saValues.length\n    : 3; // Default to middle score if nothing assessed\n  const saScore = Math.round(((saAvg - 1) / 4) * 100); // 1→0%, 3→50%, 5→100%\n\n  // Weighted combination\n  const readiness = Math.round(\n    skillMatch * 0.50 +\n    portfolioScore * 0.2
<truncated 86 bytes>