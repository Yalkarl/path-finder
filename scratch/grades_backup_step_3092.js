"const fs = require('fs');\n\nconst filePath = 'c:\\\\Users\\\\User\\\\OneDrive\\\\Desktop\\\\Path-Finder\\\\src\\\\app\\\\setup\\\\grades\\\\page.js';\nlet content = fs.readFileSync(filePath, 'utf8');\n\n// Find the segment containing the leftover option and select elements and remove them\nconst pattern = /required\\s*\\/>\\s*<\\/div>\\s*<\\/div>\\s*<option[\\s\\S]*?<\\/select>\\s*<\\/div>\\s*<\\/div>/;\n\nif (pattern.test(content)) {\n  console.log(\"Pattern matched!\");\n  content = content.replace(pattern, 'required />\\n                    </div>\\n                  </div>');\n} else {\n  console.log(\"Pattern not matched directly, trying index-based replace\");\n  // Look for any disabled option and remove it along with the closing select/divs\n  const optionStr = 'disabled>';\n  const startIdx = content.indexOf(optionStr);\n  if (startIdx !== -1) {\n    const selectEnd = content.indexOf('</select>', startIdx);\n    if (selectEnd !== -1) {\n      const nextDiv = content.indexOf('</div>', selectEnd);\n      const nextNextDiv = content.indexOf('</div>', nextDiv + 6);\n      \n      const before = content.substring(0, startIdx - 50); // back up to the option start\n      const targetOptionStart = before.indexOf('<option');\n      const cleanBefore = content.substring(0, targetOptionStart);\n      const cleanAfter = content.substring(nextNextDiv + 6);\n      \n      content = cleanBefore + cleanAfter;\n      console.log(\"Index-based replacement done!\");\n    }\n  }\n}\n\n// Also fix the card header title typo: เกรดเฉลยสะสมรายวิชา -> เกรดเฉลี่ยสะสมรายวิชา\ncontent = content.replace('เกรดเฉลยสะสมรายวิชา', 'เกรดเฉลี่ยสะสมรายวิชา');\n// Fallback in case of encoding conversion corruption\ncontent = content.replace('เกรดเฉลยสะสมรายวิชา', 'เกรดเฉลี่ยสะสมรายวิชา');\n\nfs.writeFile
<truncated 82 bytes>